#include <iostream>

using namespace std;

int main() {
	string str = "Have a nice day";

	if (str.find("Have a nice day") != string :: npos) {
		cout << "needle in the string";
	}
	else if (str == "Have a nice day") {

	}

	if (mod 4) {
		if (century year) {
			
		}
	}

}
